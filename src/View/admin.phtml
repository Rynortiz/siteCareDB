<h1>Gerenciar Velas</h1>
<a href="/admin/velas/adicionar" class="btn btn-success">+ Nova Vela</a>
<table class="table mt-3">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Aroma</th>
            <th>Preço</th>
            <th>Status</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($velas as $vela): ?>
            <tr>
                <td><?= htmlspecialchars($vela->getNome()) ?></td>
                <td><?= htmlspecialchars($vela->getAroma()) ?></td>
                <td>R$ <?= number_format($vela->getPreco(), 2, ',', '.') ?></td>
                <td><?= htmlspecialchars($vela->getStatus()->value) ?></td>
                <td>
                    <a href="/editarVelas" class="btn btn-warning btn-sm">Editar</a>
                    <a href="/admin" class="btn btn-danger btn-sm"
                       onclick="return confirm('Tem certeza que deseja excluir?')">Remover</a>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>
