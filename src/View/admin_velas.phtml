<body class="container py-4">
    <h1 class="subtitulo">Gerenciar Velas</h1>

    <!-- Formulário de cadastro -->
    <div class="card mb-4">
        <div class="card-body">
            <h5 class="subsubtitulo">Adicionar Nova Vela</h5>
            <form method="POST" enctype="multipart/form-data" action="/admin/criar">
                <div class="mb-2"><input type="text" name="nome" class="form-control" placeholder="Nome" required></div>
                <div class="mb-2"><input type="text" name="aroma" class="form-control" placeholder="Aroma" required></div>
                <div class="mb-2"><input type="number" step="0.01" name="preco" class="form-control" placeholder="Preço" required></div>
                <div class="mb-2"><input type="file" accept="images/*" name="imagem" class="form-control" placeholder="URL da Imagem" required></div>
                <div class="mb-2">
                    <select name="status" class="form-control">
                        <option value="disponível">Disponível</option>
                        <option value="indisponível">Indisponível</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-success">Adicionar</button>
            </form>
        </div>
    </div>

    <!-- Listagem das velas -->
    <div class="row">
        <?php foreach ($velas as $vela): ?>
            <div class="col-md-4 mb-3">
                <div class="card" style="width: 18rem;">
                    <img src="<?= htmlspecialchars($vela->getImagem()) ?>" class="card-img-top" alt="<?= htmlspecialchars($vela->getNome()) ?>">
                    <div class="card-body">
                        <h5 class="card-title"><?= htmlspecialchars($vela->getNome()) ?></h5>
                        <p class="card-text">
                            Aroma: <?= htmlspecialchars($vela->getAroma()) ?><br>
                            Preço: R$ <?= number_format($vela->getPreco(), 2, ',', '.') ?><br>
                            Status: <?= $vela->getStatus()->value ?>
                        </p>

                        <!-- Form editar -->
                        <form method="POST" enctype="multipart/form-data" action="/admin/editar">
                            <input type="hidden" name="id" value="<?= $vela->getId() ?>">
                            <input type="text" name="nome" value="<?= htmlspecialchars($vela->getNome()) ?>" class="form-control mb-1">
                            <input type="text" name="aroma" value="<?= htmlspecialchars($vela->getAroma()) ?>" class="form-control mb-1">
                            <input type="number" step="0.01" name="preco" value="<?= $vela->getPreco() ?>" class="form-control mb-1">
                            <input type="file" name="imagem" accept="image/*" class="form-control mb-1">
                            <select name="status" class="form-control mb-2">
                                <option value="disponível" <?= $vela->getStatus()->value === 'disponível' ? 'selected' : '' ?>>Disponível</option>
                                <option value="indisponível" <?= $vela->getStatus()->value === 'indisponível' ? 'selected' : '' ?>>Indisponível</option>
                            </select>

                            <!-- Botões lado a lado -->
                            <div class="d-flex justify-content-between">
                                <button type="submit" class="btn btn-primary btn-sm me-2">Salvar</button>
                        </form>

                        <form method="POST" action="/admin/remover">
                            <input type="hidden" name="id" value="<?= $vela->getId() ?>">
                            <button type="submit" class="btn btn-danger btn-sm">Remover</button>
                        </form>
                    </div>
                </div>
            </div>
    </div>
<?php endforeach; ?>
</div>
</body>